<nav class="navbar navbar-default">
  <div class="container">
    <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toque para navegar</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Home", root_path%>
        </div>
        <div class="navbar-collapse collapse">
          <% unless current_user.blank? %>
            <ul class="nav navbar-nav navbar-form navbar-right">
              <%= form_tag(profile_search_path, :method => "get", id: "search-form", class: "navbar-nav navbar-form") do %>
                <%= text_field_tag :search, params[:search], placeholder: "Pesquisar...", class: "form-control" %>
              <%= submit_tag "Pesquisar", class: "btn btn-default" %>
              <% end %>
              <li class="dropdown navbar-right">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.first_name  %> <span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Perfil", profile_myprofile_path %></li>
                  <li role="separator" class="divider"></li>
                  <li><%= link_to "Sair" , destroy_user_session_path, method: :delete %></li>
                </ul>
              </li>
            </ul>
          <% end %>
        </div><!--/.nav-collapse -->
      </div>
    </div>
</nav>